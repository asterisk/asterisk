context test1
{
        771 => {
                for(	i=0; 
			${i} <= 3; 
			i = ${i} + 1 )
                        NoOp(i is '${i}');
        }
        772 => {
                for(i=0; ${i} <= 3;i= ${i} + 1 )
                        NoOp(i is '${i}');
        }
}
